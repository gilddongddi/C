//문자형 변수 이해하기(char)

#include<stdio.h>

main()
{
	char ch;
	unsigned char j;
	char k;

	ch = 200;
	j = 200;
	k = 'a';

	printf("문자형 변수 ch의 값을 %d\n", ch);
	printf("문자형 변수 j의 값은 %d\n", j);
	printf("문자형 변수 k의 값은 %d\n", k);
}